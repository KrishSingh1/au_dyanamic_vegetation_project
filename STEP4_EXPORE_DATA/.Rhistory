plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = df["var"]
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~df["var"]
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = var
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
View(dea.fc.sites.nearest)
?omit
?na.omit
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
) # %>%
#addLegend("bottomright", pal = col.var, values = ~var,
#          title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
) # %>%
#addLegend("bottomright", pal = col.var, values = ~var,
#          title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df$var)
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>% aus.map %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = bs,
title = "Data Values", opacity = 1)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map(dea.fc.sites.nearest, aus.map, "pv")
plot_aus_map(dea.fc.sites.nearest, aus.map, "npv")
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = dea.fc.sites.nearest$bs,
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
class(dea.fc.sites.nearest$bs)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet(dea.fc.sites.nearest) %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = dea.fc.sites.nearest$bs,
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet(dea.fc.sites.nearest) %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
col.var <- colorNumeric(palette = c("green", "red"), domain = dea.fc.sites.nearest$bs)
aus.map <- leaflet(dea.fc.sites.nearest) %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles() %>%
addCircleMarkers(
lat = ~latitude,
lng = ~longitude,
color = ~col.var(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs", " :", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~bs,
title = "Data Values", opacity = 1)
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
aus.map
plot_aus_map <- function(df, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df$var)
aus.map.var <- leaflet(df) %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, "bs")
plot_aus_map <- function(df, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df$var)
aus.map.var <- leaflet(df) %>% addTiles %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = "Data Values", opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, "bs")
plot_aus_map <- function(df, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df$var)
aus.map.var <- leaflet(df) %>% addTiles %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)  %>%
addLegend("bottomright", pal = col.var, values = ~var,
title = variable, opacity = 1)
return(aus.map.var)
}
dea.fc.sites.nearest <- na.omit(dea.fc.sites.nearest)
plot_aus_map(dea.fc.sites.nearest, "bs")
plot_aus_map(dea.fc.sites.nearest,  "pv")
plot_aus_map(dea.fc.sites.nearest, "npv")
