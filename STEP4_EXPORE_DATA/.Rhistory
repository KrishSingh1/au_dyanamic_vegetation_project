addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
label= ~paste("bs:", bs, "\n", "time", time)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
label= paste0("bs:", bs, "\n", "time", time)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
label= ~paste0("bs:", bs, "\n", "time", time)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "\n", "time: ", time)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>", "time: ", time)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
label = ~paste0("bs: " bs)
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
label = ~paste0("bs: " bs)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","diff","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
label = ~paste0("bs: " bs)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","diff","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff),
label = ~paste("bs: " bs)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest[,c("time","diff","latitude", "longitude","bs")],
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest],
australia_map
library(leaflet)
library(dplyr)
center_latitude <- -25.2744
center_longitude <- 133.7751
australia_map <- leaflet() %>%
setView(lng = center_longitude, lat = center_latitude, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
australia_map <- australia_map %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
australia_map
cen.lat <- -25.2744
cen.lng <- 133.7751
aus.map <- leaflet() %>%
setView(lng = cen.lng, lat = cen.lat, zoom = 5) %>%
addTiles()
colour_gradient <- colorNumeric(palette = c("red", "green"), domain = dea.fc.sites.nearest$bs)
aus.map.bs <- aus.map %>%
addCircleMarkers(data = dea.fc.sites.nearest,
lat = ~latitude,
lng = ~longitude,
color = ~colour_gradient(bs),
radius = 3,
fillOpacity = 1,
popup= ~paste0("bs: ", bs, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
aus.map.bs
plot_aus_map <- function(df, aus.map, variable){
col.var <- colorNumeric(palette = c("red", "green"), domain = df[variable])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(df[variable]),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", df$variable, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map <- function(df, aus.map, variable){
col.var <- colorNumeric(palette = c("red", "green"), domain = df[variable])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(df[variable]),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", df$variable, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
dea.fc.sites.nearest["bs"]
plot_aus_map <- function(df, aus.map, variable){
col.var <- colorNumeric(palette = c("red", "green"), domain = df[[variable]])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(df[[variable]]),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", df$variable, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[[variable]]
col.var <- colorNumeric(palette = c("red", "green"), domain = var)
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = var)
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map(dea.fc.sites.nearest, aus.map, "pv")
plot_aus_map(dea.fc.sites.nearest, aus.map, "npv")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,
"site_unique", site_unique)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,
"site_unique", "<br>", site_unique)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("green", "red"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)
return(aus.map.var)
}
TIME <- read.csv("/Users/krish/Desktop/DYNAMIC MODEL VEGETATION PROJECT/DataExtraction/BACKUP_DATA/csv_files/QDACYP0018.csv")
plot(x = TIME$time, y = aggregate.data.frame(TIME[,c("time", "bs")] ,by = TIME$time)["bs"])
plot(x = TIME$time,
y = aggregate.data.frame(TIME[,c("time", "bs")] ,by = TIME$time, FUN = mean, na.rm = T)["bs"])
plot(x = TIME$time,
y = aggregate.data.frame(TIME[,c("time", "bs")] ,by = list(TIME$time), FUN = mean, na.rm = T)["bs"])
df <- y = aggregate.data.frame(TIME[,c("time", "bs")] ,by = list(TIME$time), FUN = mean, na.rm = T)
df <- y = aggregate(TIME[,c("time", "bs")],by = list(TIME$time), FUN = mean, na.rm = T)
df <- y = aggregate(TIME["bs"],by = list(TIME$time), FUN = mean, na.rm = T)
View(TIME)
df <- aggregate(TIME["bs"],by = list(TIME$time), FUN = mean, na.rm = T)
df
plot(x = df$time,
y = df$bs)
View(df)
plot(x = df$Group.1, y = df$bs)
plot(x = as.Date(df$Group.1), y = df$bs, )
plot(x = as.Date(df$Group.1), y = df$bs, "-")
plot(x = as.Date(df$Group.1), y = df$bs, ".")
plot(x = as.Date(df$Group.1), y = df$bs, pch = 1 )
plot(x = as.Date(df$Group.1), y = df$bs, pch = 2 )
plot(x = as.Date(df$Group.1), y = df$bs, pch = 3 )
plot(x = as.Date(df$Group.1), y = df$bs, pch = 1 )
plot(x = as.Date(df$Group.1), y = df$bs, pch = 1, type "l" )
plot(x = as.Date(df$Group.1), y = df$bs, pch = 1, type = "l" )
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
abline(x = "2018-07-21")
plot(x = as.Date(df$Group.1), y = df$bs, pch = 1, type = "l" )
abline(x = "2018-07-21")
abline(x = as.Date("2018-07-21"))
abline(v = as.Date("2018-07-21"))
abline(v = as.Date("2018-07-21"), colour = "red")
abline(v = as.Date("2018-07-21"), col = "red")
plot_aus_map(dea.fc.sites.nearest, aus.map, "bs")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "pv")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
) %>% addLegend()
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "pv")
plot_aus_map <- function(df, aus.map, variable){
df["var"] <- df[variable]
col.var <- colorNumeric(palette = c("red", "green"), domain = df["var"])
aus.map.var <- aus.map %>%
addCircleMarkers(data = df,
lat = ~latitude,
lng = ~longitude,
color = ~col.var(var),
radius = 3,
fillOpacity = 1,
popup= ~paste0(variable, " :", var, "<br>",
"time: ", time, "<br>",
"diff: ", diff,"<br>",
"site_unique", site_unique)
) %>% addLegend(colors = col.var, values = var)
return(aus.map.var)
}
plot_aus_map(dea.fc.sites.nearest, aus.map, "pv")
View(dea.fc.sites.nearest)
range(dea.fc.sites.nearest["bs"])
range(dea.fc.sites.nearest["bs"], rm.na = T)
range(dea.fc.sites.nearest["bs"], na.rm = T)
